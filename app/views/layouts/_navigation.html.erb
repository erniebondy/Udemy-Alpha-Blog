<nav class="navbar navbar-expand-lg bg-body-tertiary">
  <div class="container-fluid">
    <%= link_to "ALPHA BLOG", root_path, class: "navbar-brand text-uppercase fw-bold" %>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    
    <div class="collapse navbar-collapse" id="navbarSupportedContent">

      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        
        <% if logged_in? %>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              Profile [<%= current_user.username %>]
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to "View profile", user_path(current_user), class: "nav-link" %></li>
              <li><%= link_to "Edit profile", edit_user_path(current_user), class: "nav-link" %></li>
              <li>
                <%= link_to "Delete profile", user_path(current_user), class: "nav-link text-danger", 
                            data: { turbo_method: :delete, turbo_confirm: "Are you sure?" }
                %>
              </li>
            </ul>
            <li><%= link_to "Log Out", logout_path, class: "nav-link", data: { turbo_method: :delete } %></li>
          </li>
        <% else %>
          <li class="nav-item"><%= link_to "Log In", login_path, class: "nav-link" %></li>
          <li class="nav-item"><%= link_to "Sign Up", signup_path, class: "nav-link" %></li>
        <% end %>

        <li class="nav-item">
          <%= link_to "Bloggers", users_path, class: "nav-link" %>
        </li>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            Articles
          </a>
          <ul class="dropdown-menu">
            <% if logged_in? %>
              <li><%= link_to "Create new article", new_article_path, class: "dropdown-item" %></li>
            <% end %>
            <li><%= link_to "View articles", articles_path, class: "dropdown-item" %></li>
            <%# <li><hr class="dropdown-divider"></li> %>
            <%# <li><a class="dropdown-item" href="#">Something else here</a></li> %>
          </ul>
        </li>
      </ul>

      <form class="d-flex" role="search">
        <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search"/>
        <button class="btn btn-outline-success" type="submit">Search</button>
      </form>

    </div>
  </div>
</nav>